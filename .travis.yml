sudo: required
language: python
services:
  - podman
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -o Dpkg::Options::="--force-confold" --force-yes -y podman
install:
  - pip install molecule
  - pip install molecule-podman
script:
  - molecule test
